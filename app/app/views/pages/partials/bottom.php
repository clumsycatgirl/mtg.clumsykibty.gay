<div class="flex items-center justify-center h-[6vh]">
    <div class="align-middle self-center">
        @if ($this->logged)
        <h6>logged</h6>
        @else
        <h6>not logged</h6>
        @endif
    </div>
</div>